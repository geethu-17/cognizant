# Module1-css3

Small CSS3 module project. Contains `index.html` and `styles.css`.

To push to GitHub:

1. Create a repository on GitHub.
2. Add the remote and push:

```powershell
git remote add origin <repo-URL>
git push -u origin main
```
